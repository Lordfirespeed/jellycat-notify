[project]
name = "jellycat-notify"
dynamic = ["version"]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.11.16",
    "discord-py>=2.5.2",
    "python-dotenv>=1.1.0",
]
